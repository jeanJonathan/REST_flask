# adapté de https://dev.to/alissonzampietro/the-amazing-journey-of-docker-compose-17lj
version: '3.8'
services:
    api-service:
        build: .
        volumes:
            - ./api/:/usr/src/app/
        ports:
            - 5000:5000
        environment:
            PORT: 5000
            FLASK_DEBUG: 1